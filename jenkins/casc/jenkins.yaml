jenkins:
  systemMessage: "go-micro CI — Jenkins ngoài cluster. 1 job / service. CD = Argo CD."
  numExecutors: 2
  mode: NORMAL
  securityRealm:
    local:
      allowsSignup: false
      users:
        - id: "${JENKINS_ADMIN_ID}"
          password: "${JENKINS_ADMIN_PASSWORD}"
  authorizationStrategy:
    loggedInUsersCanDoAnything:
      allowAnonymousRead: false

credentials:
  system:
    domainCredentials:
      - credentials:
          - usernamePassword:
              id: "dockerhub-credentials"
              scope: GLOBAL
              description: "Docker Hub (CI push images)"
              username: "${DOCKERHUB_USER}"
              password: "${DOCKERHUB_TOKEN}"
          - usernamePassword:
              id: "github-go-micro-pat"
              scope: GLOBAL
              description: "GitHub PAT (clone service repos + push gitops)"
              username: "${GITHUB_USER}"
              password: "${GITHUB_PAT}"

unclassified:
  location:
    url: "${JENKINS_URL}"
  globalLibraries:
    libraries:
      - name: "go-micro-ci"
        defaultVersion: "main"
        implicit: false
        allowVersionOverride: true
        includeInChangesets: false
        retriever:
          modernSCM:
            scm:
              git:
                remote: "https://github.com/minhtri1612/go-micro-pipeline-lib.git"
                credentialsId: "github-go-micro-pat"

jobs:
  - file: /var/jenkins_home/casc_configs/jobs.groovy
