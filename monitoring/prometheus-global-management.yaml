apiVersion: v1
kind: Service
metadata:
  name: prometheus-global
  namespace: monitoring
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: 9090
  selector:
    app.kubernetes.io/name: prometheus
