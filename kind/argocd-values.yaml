repoServer:
  networkPolicy:
    create: false

controller:
  networkPolicy:
    create: false

server:
  networkPolicy:
    create: false
  service:
    type: NodePort
    nodePortHttps: 30443

applicationSet:
  networkPolicy:
    create: false

notifications:
  networkPolicy:
    create: false

redis:
  networkPolicy:
    create: false

configs:
  params:
    reposerver.metrics.listen.address: "0.0.0.0"
