# Kind cluster: dev
# API server bound to 127.0.0.1:30443 (avoid kubeconfig https://0.0.0.0 which breaks TLS).
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
networking:
  apiServerAddress: "127.0.0.1"
  apiServerPort: 30443
  # kindnet (default).
  podSubnet: 10.201.0.0/16
  serviceSubnet: 10.221.0.0/16

nodes:
  - role: control-plane
    image: kindest/node:v1.28.0
